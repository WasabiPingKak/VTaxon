// ── LOD thresholds ──
export const LOD_DOTS_ONLY = 0.2;

// ── Taxonomy node radius (area-proportional) ──
export const MIN_R = 5;
export const MAX_R = 30;

// ── Scale-compensated font system ──
export const FONT_MIN_SCALE = 0.55;
export const FONT_FAMILY = '"Microsoft JhengHei", "Noto Sans TC", sans-serif';

// ── Label width cap for species/breed rect ──
export const SPECIES_MAX_RECT_W = 184;  // MAX_LABEL_W.SPECIES(160) + 24 padding
export const BREED_MAX_RECT_W = 140;    // MAX_LABEL_W.BREED(120) + 20 padding
