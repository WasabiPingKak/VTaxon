repos:
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        exclude: 'node_modules/|frontend/dist/'
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.0
    hooks:
      - id: ruff
        args: ['--fix', '--config', 'backend/pyproject.toml']
        files: ^backend/
      - id: ruff-format
        args: ['--config', 'backend/pyproject.toml']
        files: ^backend/
  - repo: local
    hooks:
      - id: frontend-lint
        name: Run ESLint
        entry: bash -c "cd frontend && npx eslint src/ --no-warn-ignored"
        language: system
        pass_filenames: false
        files: ^frontend/src/
      - id: frontend-tsc
        name: tsc (frontend type check)
        entry: bash -c "cd frontend && npx tsc --noEmit"
        language: system
        pass_filenames: false
        files: ^frontend/src/
      - id: backend-mypy
        name: mypy (backend type check)
        entry: bash -c "cd backend && python -m mypy app/ --config-file pyproject.toml"
        language: system
        pass_filenames: false
        files: ^backend/
      - id: backend-tests
        name: Run backend tests
        entry: bash -c "cd backend && python -m pytest tests/ -x -q"
        language: system
        pass_filenames: false
        files: ^backend/
